# dbmock 数据真实性审计与整改计划

审计日期：2026-08-06

审计对象：修复前 Doris `ecommerce` 数据库中的完整两年数据，以及当时的 `dbmock` 生成逻辑

修复前数据时间范围：2024-08-06 至 2026-08-06

最终验收数据时间范围：2024-08-07 至 2026-08-07

## 整改执行结果

生成模型版本 4 已于 2026-08-07 完成两年全量生成和验收，本文后续问题描述保留为修复前审计证据。

- 45 张表共 24,568,329 行
- 全量质量报告状态为 `passed`
- 硬错误 0，分布警告 0
- 会话内事件时间冲突 0
- 订单金额、优惠分摊、支付、配送、退款和库存数量不匹配 0
- 购物车会话下单转化率 42.55%
- 购物车变更事件率 7.85%
- 取消收藏事件率 10.59%
- 缺货取消率 1.46%，全部由真实库存分配失败触发
- 零可售库存 SKU 日占比 0.076%
- 在途库存 SKU 日占比 0.894%
- 总优惠率 9.74%
- 月度转化率变异系数 5.49%
- 沉默或流失用户占比 6.16%
- 订单状态跨月 4,443 个，配送状态跨月 3,823 个，退款状态跨月 242 个

整改计划中的质量护栏、会话时间线、购物车与收藏状态、库存原子预占、配送闭环、滚动生命周期、营销和条件化耗时均已实现并通过全量数据验证。

平台画像已在 README 中明确为偏 3C、服饰和家居的综合型电商，保留真实商品目录形成的品类结构，不再把品类均匀度作为真实性目标。

## 一、修复前审计结论

修复前的数据已经具备可用的数仓骨架，维度、交易、支付、履约、退款、库存之间的大部分外键和金额关系能够闭环，月度季节性、渠道差异、品类结构也已经存在。

但修复前的这批数据不能定义为“业务真实性验收通过”。主要问题不是表之间完全断裂，而是生成规则留下了明显的程序化痕迹：

1. 会话结束时间早于部分页面、加购和下单事件
2. 加购会话与下单会话完全重合，没有真实的购物车放弃、修改和清空过程
3. 缺货取消并非库存不足驱动，取消前反而已经成功预占库存
4. 少量已发货订单明细没有对应的正向发货数量
5. 月、星期和小时转化率过于稳定，订单仍然带有按配额生成的特征
6. 用户生命周期使用累计会话数判断，导致长期未活跃用户仍被标记为正常
7. 优惠金额占成交金额比例过低，积分始终未使用，营销链路不完整
8. 支付、履约、页面停留等耗时使用有限模板，分布离散度不足
9. 库存总体过于充足，日快照没有出现可售库存为零的 SKU

现有质量报告显示 `passed`，只能说明现有校验规则通过，不能覆盖上述真实性问题。需要先补充规则，再修改生成模型，最后重新生成全量数据验收。

## 二、数据规模与总体分布

### 2.1 当前规模

| 指标 | 当前值 |
| --- | ---: |
| 表数量 | 45 |
| 总行数 | 24,294,723 |
| 用户数 | 30,000 |
| SPU 数 | 5,000，另有 1 条未知成员 |
| SKU 数 | 31,554，另有 1 条未知成员 |
| 会话数 | 1,250,355 |
| 页面浏览数 | 4,000,000 |
| 搜索数 | 600,000 |
| 搜索点击数 | 442,021 |
| 加购事件数 | 100,000 |
| 收藏事件数 | 68,972 |
| 订单数 | 65,840 |
| 订单明细数 | 100,000 |
| 支付尝试数 | 66,120 |
| 配送记录数 | 70,135 |
| 退款明细数 | 5,318 |
| 评论数 | 19,803 |
| 库存流水数 | 237,895 |
| 日快照数 | 15,705,893 |

### 2.2 基本合理的部分

- 订单量在 11 月、6 月等促销月份存在季节性抬升
- 当前日期附近存在未完成订单，而不是所有订单都被强制终态化
- 渠道转化率有明显差异：APP 约 6.88%，小程序约 5.96%，PC 约 5.28%，H5 约 3.37%，SEM 约 1.33%
- 支付方式占比大致为微信 40.37%、支付宝 35.20%、花呗 13.68%、银联 10.00%、货到付款 0.75%
- 订单退款率约 8.08%，退款拒绝或取消占比约 11.21%，负面评论占比约 8.27%
- 自营订单占比约 19.40%，头部品牌没有过度集中
- 商品价格均值约 874 元，中位数约 138 元，P90 约 2,221 元，P99 约 10,689 元，电子品类拉高整体客单价的结构可以解释
- 订单平均 1.52 个明细、2.44 件商品，中位客单价约 396 元，P90 约 5,978 元
- 当前订单状态以已完成为主，并保留了已支付、已发货和刚创建的近期订单

这些指标说明宏观结构已经成形，但不能抵消微观链路中的确定性冲突。

## 三、问题清单与优先级

### P0：必须先修复的正确性问题

#### P0-1 会话时间边界冲突

发现：

- 54,769 条页面浏览发生在 `session_end_time` 之后，延迟中位数约 10 秒，P95 约 42 秒，最大约 97 秒
- 34,082 条加购事件发生在会话结束后，延迟中位数约 12 秒，最大约 13 秒
- 99,770 条订单明细对应的下单时间晚于来源会话结束时间，绝大多数固定晚 30 秒
- 搜索和搜索点击暂未发现越过会话边界
- 用户注册、支付、履约、退款和评论相对订单时间暂未发现反向冲突
- 同一业务对象内部的状态事件顺序和序号检查通过

影响：

- 无法解释“会话已经结束后用户继续浏览、加购和下单”
- 会影响漏斗分析、会话归因、转化时长和路径分析
- 属于数据正确性错误，不只是分布不够自然

根因：

- `src/batches/behavior.py` 先按随机时长计算会话结束时间
- 页面事件按固定步长继续向后排布，可能超过预先计算的结束时间
- 加购被安排在会话结束前约 20 秒
- 下单被固定安排在会话结束后 30 秒
- 会话记录在完整行为链路确定之前就已经生成

修改方案：

1. 在 `src/timeline.py` 建立统一的 `SessionJourney`，集中保存会话开始、页面、搜索、点击、收藏、购物车、结算和下单时间
2. `src/batches/behavior.py` 先规划并产生行为事件，不再提前写入会话结束时间
3. `src/batches/commerce.py` 从有效购物车状态创建订单，并返回实际下单时间和最后一个会话内事件时间
4. 当日行为和交易完成后再生成会话汇总，`session_end_time` 取最后一个会话内事件时间加合理尾部停留时间
5. 转化页面顺序调整为“商品详情 → 购物车 → 结算页 → 下单成功”，不再使用“订单页 → 结算页”的逆序
6. 所有会话内事件必须满足 `session_start_time <= event_time <= session_end_time`

验收标准：

- 页面、搜索、点击、收藏、购物车、结算和下单越界数量全部为 0
- 下单会话的退出页面和最后页面符合路径定义
- 会话持续时间等于实际事件跨度加尾部停留时间，不允许通过扩大结束时间掩盖异常

#### P0-2 购物车漏斗不真实

发现：

- 加购会话数为 65,840，与订单会话数和订单数完全一致
- 100,000 条加购事件与 100,000 条订单明细完全一致
- 所有购物车事件都是“加入”，没有删除商品、修改数量或清空购物车
- 所有收藏事件都是“收藏商品”，没有取消收藏
- `CART`、`CHECKOUT`、`ORDER` 三类页面均恰好为 65,840 条

影响：

- 不存在购物车放弃、跨会话购物车、商品数量修改、取消收藏等真实行为
- 漏斗结果可以直接反推出生成配额
- 购物车、订单和页面路径虽然外键闭合，但业务含义不真实

根因：

- 当前先选出需要转化的会话，再为这些会话生成购物车和订单
- 多明细订单在交易阶段补写对应加购事件
- 购物车是订单的附属记录，而不是独立业务状态

修改方案：

1. 在业务状态中增加按“用户 + SKU”维护的购物车和收藏状态
2. 浏览、搜索、收藏和加购先独立发生，订单只能从当时有效的购物车中转化
3. 支持购物车加入、数量变更、删除和清空，支持收藏与取消收藏
4. 允许购物车跨会话、跨天和跨月保留，并设置合理的自然失效或用户清理行为
5. 订单量通过转化概率校准逼近目标，不再通过让全部加购会话下单来满足精确配额
6. 每日根据剩余目标和剩余天数动态调整全局转化截距，但不改变用户、渠道、商品和活动之间的相对差异

初始目标范围：

- 有购物车行为的会话转化为订单的比例为 20% 至 55%
- 购物车放弃会话占比不低于 20%
- 删除、数量变更和清空事件合计占购物车事件的 5% 至 30%
- 取消收藏占收藏状态变更事件的 5% 至 25%
- 页面、购物车、订单数量之间不得保持精确固定倍数

上述范围是本项目的初始校准目标，不作为所有电商平台的通用行业结论。

#### P0-3 缺货取消与库存事实矛盾

发现：

- 805 个最新状态为缺货取消的订单均出现过成功的销售预占
- 对应 1,226 条预占事件增加了 1,964 件预占库存，随后又由释放事件冲回
- 15,705,893 条日快照中，可售库存为 0 的比例为 0
- 初始库存约 347.76 万件，而销售发货减少约 15.41 万件，库存供给明显过宽

影响：

- “缺货”由随机数决定，不是库存状态导致
- 库存预占、取消原因和快照相互矛盾
- 无法用于库存周转、缺货率、安全库存和补货分析

根因：

- `src/batches/commerce.py` 在生成预占流水后，再随机选择约 1.2% 的订单标记为缺货取消
- 库存预占没有以可售数量校验结果作为订单能否成立的前置条件

修改方案：

1. 在底层库存状态中实现原子预占：只有 `available_qty >= requested_qty` 时才产生成功预占
2. 预占失败时直接返回库存分配失败结果，不写正向预占流水
3. 只有显式模拟并发抢占时，才允许出现检查通过但提交失败，并记录对应的库存竞争原因
4. 初始库存按 SKU 销量、补货周期和安全库存天数生成，不再使用过宽的统一库存范围
5. 补货由需求预测、再订货点和采购提前期触发，让缺货、低库存、在途和到货能够自然出现
6. 订单取消原因由实际业务条件决定，不能从固定概率直接抽取“缺货”

验收标准：

- 每个缺货取消明细都能追溯到预占时的库存不足或显式并发竞争事件
- 不允许成功预占后无证据地标记为缺货
- 日快照中存在合理的低库存和零可售库存，但不出现大面积长期缺货
- 初始校准目标：零可售 SKU 日占比 0.1% 至 3%，在途 SKU 日占比 0.5% 至 5%

#### P0-4 已发货状态与配送数量不闭环

发现：

- 5 条最新状态为已发货的订单明细没有对应的正向配送数量
- 这些记录集中在 2026-08-04 至 2026-08-05 的近期订单
- 其他主要订单、支付、退款、评论、库存和价格外键未发现孤儿记录

影响：

- 订单状态已经进入已发货，但配送事实无法证明实际发出了多少件
- 发货件数、履约率和在途数量统计不一致

修改方案：

1. 包裹和配送明细必须先成功生成，随后才能写入 `SHIPPED` 状态事件
2. 状态迁移函数接收配送结果，不再分别随机决定状态和配送事实
3. 截止日来不及发货的订单保持已支付或待发货状态
4. 退货配送与正向配送分开核对方向和数量

验收标准：

- 每条已发货或已完成订单明细的正向配送数量等于有效发货数量
- 已支付但未发货订单允许不存在配送事实
- 关联数量不匹配数为 0

### P1：影响真实性和分析价值的问题

#### P1-1 转化率随时间过于稳定

发现：

- 月度会话转化率仅在约 5.20% 至 5.33% 之间波动
- 小时订单／浏览比大多集中在约 1.56% 至 1.96%
- 星期维度订单／浏览比约为 1.63% 至 1.66%
- 渠道差异存在，但月、星期和小时差异被配额分摊明显抹平
- 每个会话的平均搜索次数在不同会话长度下仍接近 0.48

根因：

- 流量、搜索和订单目标使用相近权重按天分摊
- 每日按精确数量抽取转化会话，订单不是行为结果

修改方案：

1. 只对总流量和大盘规模设置软目标，不对每天订单设置刚性配额
2. 转化概率同时考虑用户阶段、渠道、设备、星期、小时、品类、价格、活动、库存、搜索、停留和购物车状态
3. 促销期同时影响流量和转化率，而不是只增加当天数据量
4. 使用月度全局截距校准最终两年订单规模，保留各特征之间的真实差异
5. 搜索次数与会话意图和会话长度相关，不再独立按统一概率生成

验收标准：

- 大促期相对相邻基线期的转化率和客单价存在可解释变化
- 高意向路径转化率高于普通浏览路径
- 月、星期和小时分布不存在由精确配额造成的异常平坦
- 同一随机种子可复现，改变种子后统计分布保持稳定但明细不相同

#### P1-2 用户生命周期失真

发现：

- 正常用户 29,736，占 99.12%；休眠用户 248；流失用户 16
- 748 个用户从未产生会话，其中 674 个仍标记为正常
- 500 个 91 至 180 天未活跃用户仍为正常
- 7,400 个 31 至 90 天未活跃用户仍为正常
- 买家数 21,968，占用户数约 73.23%；复购买家率约 71.47%

根因：

- 生命周期同时使用不活跃天数和“累计会话数小于 3”判断休眠
- 历史上活跃过的用户累计会话数不断增加，即使长期不活跃也很难进入休眠或流失

修改方案：

1. 在业务状态中维护滚动 30、60、90 和 180 天的访问、下单、支付及消费指标
2. 生命周期改为新客、活跃、休眠、流失和召回，并按注册时间与最近活动共同判断
3. 老注册且从未活跃的用户不能继续标记为正常
4. 用户等级由滚动消费和历史价值共同决定，避免等级只升不降或高等级比例过高
5. 跨月保留滚动状态，使月初判定能够使用上月及更早活动

初始校准目标：

- 活跃或正常用户 65% 至 90%
- 休眠用户 5% 至 25%
- 流失用户 2% 至 15%
- 召回必须由休眠或流失后再次发生有效活动触发

最终比例应结合平台定位、获客速度和两年用户增长曲线再校准。

#### P1-3 营销与优惠链路偏弱

发现：

- 活动命中明细约 11.29%，优惠券命中明细约 5.05%
- 约 84.35% 的订单明细没有优惠
- 活动优惠约 21.83 万元、优惠券优惠约 5.89 万元，而销售金额约 1.596 亿元，总优惠率仅约 0.17%
- 积分抵扣始终为 0
- 4,854 个已完成优惠券分摊记录的 `USED` 事件固定晚于 `coupon_use_time` 240 秒

影响：

- 大促月份虽然订单量增加，但优惠事实不足以解释成交变化
- 优惠券分摊时间与生命周期事件时间语义不一致
- 保留但永远为 0 的积分字段会误导分析使用者

修改方案：

1. 区分商品挂牌价、实际售价、活动优惠、店铺券、平台券、积分和运费优惠
2. 优惠券先经历发放、领取、锁定、使用、释放或过期，再与订单分摊关联
3. `coupon_use_time` 记录真实 `USED` 事件时间；如原字段实际表达锁定时间，则直接更正字段定义和表结构，不保留旧接口别名
4. 若保留积分字段，则实现积分获取、余额、抵扣和退回；否则从模型中删除该无效字段
5. 营销活动需要影响曝光、点击、转化和客单价，并区分平台与商家承担金额

初始校准目标：

- 总优惠金额占优惠前成交金额 3% 至 12%
- 有任意优惠的订单明细占比 20% 至 60%
- 若启用积分，积分抵扣订单占比 1% 至 10%
- 优惠券分摊的使用时间与生命周期 `USED` 事件时间完全一致

#### P1-4 时间耗时使用有限模板

发现：

- 支付延迟只有 32 个离散取值，主要落在 183 至 378 秒
- 发货延迟只有 33 个离散取值，主要落在 8 至 40 小时
- 配送延迟只有 7 个离散取值，主要为 24 小时整数倍
- 优惠券使用事件固定晚 240 秒
- 不同页面类型的平均停留时间都在约 71.4 至 71.6 秒，几乎没有语义差异

修改方案：

1. 支付耗时采用按支付方式、设备、订单金额区分的偏态分布
2. 发货耗时考虑自营／第三方、仓库、下单时刻、工作日和节假日
3. 配送耗时考虑地区距离、物流公司、服务等级和异常事件
4. 页面停留时间按页面类型、商品品类、设备和用户意图使用不同分布
5. 所有时长从连续或高基数分布采样，避免固定分钟和整小时模板

验收标准：

- 各耗时字段具备合理的 P50、P90、P95 和长尾
- 商品详情、搜索结果、购物车、结算页的停留时长分布可区分
- 时长离散值数量随样本规模增长，不被少量模板限制
- 工作日、节假日和跨月履约均遵循同一业务日历

#### P1-5 库存和补货活性不足

发现：

- SKU 日库存平均约 109.79 件，中位数约 108，P90 约 180，P99 约 199
- 零可售库存比例为 0
- 存在在途库存的日快照比例仅约 0.046%
- 采购单约 1,783 笔、收货约 1,778 笔，截止日保留 5 笔未到货采购单

修改方案：

1. 按 SKU 销速和价值分层设置库存覆盖天数
2. 高价值慢销 SKU 使用低库存，快销 SKU 使用较高安全库存和更频繁补货
3. 采购提前期按供应商、品类和地区生成，并允许延期、部分收货和取消
4. 补货触发基于再订货点，不按固定数量随机生成
5. 快照由当日库存状态直接汇总，保持期初、入库、预占、释放、出库、调整和期末闭合

### P2：需要结合平台定位继续校准的问题

#### P2-1 品类和客单价结构

当前订单明细中，服饰约占 38.09%，家居约占 23.98%，手机约占 9.98%；GMV 中手机约占 38.91%，电脑约占 23.32%，家居约占 15.23%，服饰约占 10.87%。美妆、食品和医药占比较低。

该结构可以解释较高的平均客单价，但更像偏 3C、服饰和家居的平台，不是完全均衡的综合电商。需要在 README 中明确平台画像，或重新调整采集商品的品类配额和流量偏好。

建议先确定目标画像：

- 综合型但偏 3C
- 综合型但偏服饰家居
- 品类相对均衡

未确定画像前，不应只为追求均匀而机械调整各品类占比。

## 四、已经通过的关联与数值校验

以下关系当前未发现异常，改造时必须防止回归：

- 订单挂牌金额、销售金额、应收金额的算术关系
- 活动、优惠券、支付和包裹分摊金额关系
- 成功支付金额与订单应收金额关系
- 退款数量、退款金额和各退款组成金额关系
- 库存流水的数量、结存数量和成本计算
- 库存快照的期初、变动、期末和成本关系
- 订单、支付、配送、退款、评论、库存和价格维度的主要外键关系
- 订单、支付、配送和退款状态链顺序及事件序号
- 业务日期键与时间字段日期的一致性
- 入仓时间没有统一写成任务执行当前时间

跨月状态当前能够在同一次生成任务内继承，已观测到：

- 9,711 个订单存在跨月状态迁移
- 8,265 个配送对象存在跨月状态迁移
- 264 个退款对象存在跨月状态迁移

移除检查点功能后，跨月状态仍应在单次进程的内存业务状态中连续继承；只是不再支持任务中断后从指定月份恢复。生成失败时重新运行全量任务即可。

## 五、实施顺序

### 阶段 A：先补齐质量护栏

涉及文件：

- `src/quality.py`
- `tests/test_timeline.py`
- 新增必要的业务链路测试文件

工作项：

1. 增加所有会话事件的开始／结束边界检查
2. 增加购物车事件类型、放弃率和购物车到订单转化率检查
3. 增加收藏与取消收藏状态闭合检查
4. 增加缺货取消的库存证据检查
5. 增加已发货明细与正向配送数量闭合检查
6. 增加优惠券分摊时间与生命周期时间一致性检查
7. 增加用户生命周期与最近活动时间一致性检查
8. 增加支付、发货、配送和页面停留时长的分布检查
9. 增加低库存、零库存和在途库存暴露率检查
10. 保留现有全部金额、外键、SCD、状态链和快照算术检查

要求：

- 新规则在旧数据上应明确失败并给出异常数量和样例
- 质量报告区分“硬错误”和“分布警告”
- 硬错误必须为 0；分布警告输出实际值、目标区间和偏差
- 大表检查先按会话、订单或 SKU 聚合后关联，避免直接展开数千万行造成 Doris 内存压力

### 阶段 B：统一会话、购物车和转化时间线

涉及文件：

- `src/timeline.py`
- `src/batches/behavior.py`
- `src/batches/commerce.py`
- `main.py`

工作项：

1. 从最底层定义完整的会话旅程和购物车状态
2. 行为批次先生成非交易行为和可转化意图
3. 交易批次只消费有效购物车并返回最终交易时间
4. 所有事件完成后统一落会话汇总
5. 购物车和收藏状态在每日、每月之间继承
6. 删除基于“已选中转化会话”的旧加购生成逻辑，不增加兼容层或旧接口别名

完成标志：P0-1 和 P0-2 的所有验收规则通过。

### 阶段 C：库存驱动交易和履约

涉及文件：

- `src/timeline.py`
- `src/batches/commerce.py`
- `src/batches/products.py`

工作项：

1. 实现库存校验、预占、释放和扣减的原子状态转换
2. 缺货取消改为库存结果驱动
3. 配送事实成功后才能进入已发货状态
4. 重做初始库存、安全库存、采购和到货逻辑
5. 保证跨日、跨月的预占、发货、退货和补货状态连续

完成标志：P0-3、P0-4 和库存快照闭合规则全部通过。

### 阶段 D：重做概率模型和用户生命周期

涉及文件：

- `src/timeline.py`
- `src/batches/dimensions.py`
- `src/batches/behavior.py`
- `src/batches/commerce.py`

工作项：

1. 订单从行为倾向和购物车状态产生
2. 使用月度软校准满足总体规模，不使用每日刚性订单配额
3. 建立滚动用户活跃指标和生命周期迁移
4. 让渠道、设备、时间、用户和活动共同影响行为
5. 校准购买人数、复购率、客单价和品类结构

完成标志：时间分布不再异常平坦，生命周期与最近活动一致，整体订单规模仍接近目标。

### 阶段 E：完善营销和耗时分布

涉及文件：

- `src/batches/marketing.py`
- `src/batches/behavior.py`
- `src/batches/commerce.py`
- `src/reference.py`

工作项：

1. 完善活动、优惠券和积分生命周期
2. 统一优惠分摊时间语义
3. 使用条件化偏态分布生成支付、发货和配送耗时
4. 按页面类型生成差异化停留时长
5. 使用工作日历处理跨日、跨月和节假日履约

完成标志：优惠、积分、耗时和促销期行为达到目标区间，且金额与时间关系全部闭合。

### 阶段 F：重新生成并最终验收

执行顺序：

1. 运行单元测试和静态检查
2. 重建 Doris 业务库
3. 运行七天 smoke 数据
4. 验证所有硬错误为 0，并人工抽样典型链路
5. 运行完整两年数据生成
6. 执行全量质量报告
7. 对普通订单、缺货订单、支付失败重试、部分退款、跨月履约、跨月退款、购物车放弃和召回用户分别抽样
8. 将最终统计区间和平台画像写入 README

最终硬性验收：

- 所有会话事件时间边界冲突为 0
- 所有业务状态反向时间冲突为 0
- 所有核心外键孤儿记录为 0
- 所有金额、数量、库存和分摊差异为 0
- 缺货取消均有真实库存证据
- 已发货和已完成明细均有足量正向配送事实
- 优惠券使用时间与生命周期事件一致
- 业务状态能够跨日和跨月延续
- 两年数据截止到任务执行日期，未来事件只允许作为明确的计划时间存在，不能写成已发生事实
- 入仓时间反映对应批次的合理到达时间，不允许全部等于任务当前时间

最终分布验收：

- 购物车、收藏、订单和页面数量不存在机械等量关系
- 购物车放弃、修改、删除和清空行为达到项目目标区间
- 月、星期、小时、渠道和活动期转化差异可以被业务因素解释
- 用户生命周期与注册时间、最近活跃和滚动价值一致
- 优惠、积分、库存、支付和履约耗时具备合理长尾
- 品类、客单价和 GMV 结构符合 README 中明确的平台画像

## 六、实施约束

- 保持“两年、截止任务执行日期”的时间边界
- 保持按月编排、逐日演化、跨月继承状态的总体架构
- 检查点文件和 Doris 检查点表均不恢复，失败后全量重跑
- 保持 5,000 个真实采集 SPU，SKU 数量按真实采集结果保留
- 真实采集维度不得用随机内容冒充缺失字段
- 生成事实必须由可解释的业务规则和状态驱动
- 优先修正底层时间线、状态和库存抽象，再修改上层批次调用
- 不保留旧生成接口的兼容层、转发方法或别名
- 全量生成仍以十几分钟完成为性能目标，不能通过逐行查询 Doris 实现状态计算

## 七、建议的实际开发批次

建议按以下提交粒度执行，便于定位回归：

1. 质量规则与失败用例
2. 会话时间线与延迟落会话汇总
3. 独立购物车和收藏状态
4. 库存原子预占与缺货原因
5. 配送数量和订单状态闭合
6. 用户滚动生命周期
7. 转化概率与月度软校准
8. 营销生命周期和优惠金额
9. 条件化耗时分布
10. smoke 校准、全量生成和最终验收报告

不得一次性混合修改所有数据域后再验证。每个批次都应先通过针对性测试，再进入下一批次。
